---
import { Modal } from "accessible-astro-components";
import BookinForm from "../../forms/BookinForm.svelte";

const { bookings, max, id } = Astro.props;
---

<Modal triggerId="modal1-trigger" title="">
  <BookinForm client:load bookings={bookings ?? []} max={max} id={id} />
</Modal>
